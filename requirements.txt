# Core (required)
pydantic>=2.0

# Optional — better embeddings (falls back to HashingEmbedder without this)
sentence-transformers>=2.2

# Optional — Claude LLM backend (falls back to ExtractiveAgent without this)
anthropic>=0.25

# Optional — better vector store (falls back to InMemoryVectorStore without this)
# chromadb>=0.4

# Dev / testing
pytest>=7.0
